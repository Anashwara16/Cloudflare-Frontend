import { Link } from 'react-router-dom';

const Help = () => {
    return (
        <div>
            <h4>General Assignment CloudFlare</h4>
            <Link to="/">Go Back</Link>
        </div>
    );
};

export default Help;
